{
  "version": 2,
  "builds": [
    {
      "src": "dist-server/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "dist/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "dist-server/server.js" },
    { "src": "/(.*)", "dest": "dist/index.html" }
  ]
}
